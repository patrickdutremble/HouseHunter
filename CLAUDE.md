@AGENTS.md

## Task-specific runbooks

- **Listing extraction** ("extract data for the new listings"): read `docs/extraction-runbook.md` before starting. Do NOT read src/ files — the runbook is self-contained.
- **Bookmarklet work**: read `docs/bookmarklet-runbook.md` before starting. Do NOT re-explore the Centris DOM or app code.
- **Other tasks**: read source files as needed. The "don't re-explore" rules only apply to the two runbooks above.
